# Shell / terminal customizations

- [bash-completion-setup.md](bash-completion-setup.md) — tab-completion
  investigation for Ghostty/bash: what's already covered by shipped
  completions, custom scripts added for unpackaged tools (`gaze`, `claude`,
  `bat`), and why predictive ghost-text (`ble.sh`) was tried and reverted.
- `completions/` — raw copies of the hand-written/self-generated completion
  scripts, kept as a restore point (real copies live in
  `~/.local/share/bash-completion/completions/`).
